#include "calculator.h"
// branch2 test

int main() {
    print(add(10, 20));
    print(minus(10, 20));
    print(mul(10, 20));
    // test master
    // conflict test master
    // conflict test branch3
    return 0;
}